name: InvisibleFrames
version: '${version}'
main: me.truedarklord.invisibleFrames.InvisibleFrames
api-version: '1.20'
prefix: InvisibleFrames
authors: [ TrueDarkLord ]
description: Invisible frames. What more do I need to say.

permissions:
  invisibleframes.toggle:
    default: true

folia-supported: true